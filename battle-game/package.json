{
  "name": "wordfront-game",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test test/*.test.js"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "typescript": "^5.9.3"
  }
}
